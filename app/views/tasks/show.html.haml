.task-page-header
    %h2  
        = @task.title.truncate(30)
    .task-menu
        = image_tag 'Combined Shape'         
        .task-menu-content
            =link_to 'Edit', edit_task_path(id: @task.id) 
            =link_to 'Delete', task_path(id: @task.id) , data:{method: 'delete' , confirm: "destroy OK?"}
    


- if @task.eyecatch.attached?
    .task-image
        = image_tag @task.eyecatch



.board.mt-35
    .pt-20
        = @task.content

    
%h2.comment
    Comment

- @comments.each do |comment|
    .comment-wrapper 
        .comment-user 
            = image_tag comment.user.avatar_image
        .comment-content
            = comment.content


= link_to new_comment_path do
    .new-task 
        %p + Add new comment


